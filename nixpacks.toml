[phases.setup]
nixPkgs = ["nodePackages.npm", "python3", "python3Packages.pip", "gcc", "gnumake", "openssl"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install --upgrade pip",
    "python3 -m pip install --no-cache-dir --target ./backend/python_lib -r requirements.txt"
]

[phases.build]
cmds = [
    "npm run build",
    "PYTHONPATH=./backend/python_lib python3 -c 'import urllib3; print(\"Lib Verified\")'"
]

[start]
cmd = "node server.js"
