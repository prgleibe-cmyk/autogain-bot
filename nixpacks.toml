[phases.setup]
nixPkgs = ["nodePackages.npm", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["npm install", "pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
